# Session 2026-05-14 — 教授邮件澄清后的方向重整理

> **接续 WEEK_KICKOFF.md。** 写在 Hanzhang 要出差换设备的前一刻。
>
> **目的**：完整记录本次会话的关键转折、被推翻的方向、教授邮件的解读、以及"下一步等用户回来一起想清楚"的悬而未决问题。
> 出差期间继续 research 时，先读这份，再读 HANDOFF.md，再读 WEEK_KICKOFF.md。
>
> **最后更新**：2026-05-14，新会话的 Claude（本次会话）写。

---

## 0. 给下一个会话 Claude 的开场白

```
你好。我接续了上一个会话（参见 HANDOFF.md），并且这周从 WEEK_KICKOFF.md
上的"两条教授指示 + 我们的初步理解"开始推进。中间发生了几次方向调整，
最后落在一个比较朴素的提问："换 value function 之后会发生什么"。
完整的本次会话记录在这份 SESSION_2026-05-14.md 里。

请按以下顺序阅读：
(1) HANDOFF.md         — 项目历史、Approach A/C 的完整记录
(2) WEEK_KICKOFF.md    — 教授本周给的两条指示（其中指示 2 教授后来邮件澄清过）
(3) SESSION_2026-05-14.md（这份） — 本次会话的方向调整、被否定的路、当前定位

读完后告诉用户：
(a) 你认为我们这周到底卡在哪一步
(b) 你打算用什么方式帮用户继续推进（包括出差期间能做什么、不能做什么）
(c) 你建议先做哪一件事

注意：用户出差期间可能只有手机或没有完整开发环境，所以 (b) 里要考虑
"无代码环境下能干什么" —— 通常是数学推导、文档整理、想清楚 trick 怎么 apply。
```

---

## 1. 本次会话的开局状态

会话开始时的输入：
- WEEK_KICKOFF.md（用户上次会话留下的本周入口）
- 用户口头说"按 WEEK_KICKOFF 看一眼我们就开始干"
- 用户后续上传了教授的论文 `PhiBE_Q_Learning.pdf`（首作 Yutong Ren，通讯 Yuhua Zhu，即教授本人）

会话的实际目标（从用户的语气和节奏推断）：
- 不是要立刻动代码
- 而是要把"教授两条指示的真实含义"想清楚
- 再决定动什么

---

## 2. 教授的两条指示（原文回顾）

来自 WEEK_KICKOFF.md，复制在此方便查阅：

**指示 1**：
> 我们通过换算之后教授需要我们寻找一个合理的 pair (r, β) 在使模型运行的更好

用户澄清：**`r` 不是 reward，是 "rate"，与 β 通过 `r = e^(-β·Δt)` 关联**。

**指示 2**：
> 对于 function 中的 E[Q(s_(t+1), a_(t+1)) | s_t, a_t = π(s,a)] 这一项做一个泰勒展开，然后对这个展开进行求解

**指示 2 后续教授邮件回复（关键，本次会话出现的新信息）**：

> No, use the same trick from off-policy learning for the Q defined by the Bellman equation in RL, but apply it to this Q function. Give it some thoughts, if you cannot figure out how to apply, we can discuss it next time
> Yuhua

**用户对教授邮件的解读（本次会话最终确定的版本）**：

> "教授的意思是将原本 TD3-BC 架构中的 value function 替换为这个新的 value function" ——
> 也就是说，TD3-BC 的整体 off-policy 架构（target net、replay buffer 等）保持不变，
> 只把 critic 学的那个"Q 是什么"从离散 Bellman 的 Q 换成"教授给的连续时间 Q"。

**用户重要澄清**：教授说"论文里的内容是以后的事 (this is for later)"——
**这周不需要按 PhiBE-Q 论文的 Algorithm 1 改代码**。
论文是大方向参考，不是这周的实施指令。

---

## 3. 本次会话走过的弯路（必须记录，以免下个会话重蹈覆辙）

### 弯路 1：在 Q(s_{t+1}, a_{t+1}) 上对 Δa 用 J_π 展开

会话早期，本次 Claude 把 `E[Q(s_{t+1}, a_{t+1})]` 这一项做了二阶泰勒展开，其中
**把 Δa 用 π 展开成 J_π · Δs**，得到含有 actor Jacobian、cross terms、
有效 Hessian 的复杂表达。

**被否定的原因**：
- 用户给出教授白板上的版本——教授只写到 `(s_{t+1}-s_t)^T ∇_s^2 Q (s_{t+1}-s_t)` 就停
- **没有任何 ∇_a、∇_aa^2、cross term、J_π 的痕迹**
- 教授邮件 "No" 进一步确认这条路是错的

**正确的理解**：教授写到 `∇_s^2 Q` 就停是**有意的近似选择**——
只把 s 的随机性当主要扰动来源，不展开 Δa。这是 v-HJB 风格的近似，
不是 Q-HJB 完整展开。

### 弯路 2：把教授的指令理解成"实现 PhiBE-Q 论文的 Algorithm 1"

本次 Claude 读完论文后，把"教授让我们做的事"理解成：
- 在 TD3_BC_ct.py 里加 `critic_mode='phibe_q'` 开关
- 实现论文式 (28) 的 TD error：`δ = r + L_{b̂,Σ̂} V(s) + (1-β)V(s) - Q(s,a)` with `V(s) = max_a Q(s,a)`
- 复刻论文 §4.1 LQR 实验作为 sanity check
- 在血糖数据上做 (β, Δt) grid search 验证 Assumption 2 的理论预测

**被否定的原因**：
- 用户明确指出"教授说论文是以后的事"
- 教授邮件里的 "the trick" 不是论文的 Algorithm 1（那是 Q* + max_a 的复杂版本），
  而是 **TD3-BC 现有的 off-policy 架构**（target net + replay buffer）
- 用户原话："教授的意思是将原本 td3bc 架构中的 value function 替换为这个新的 value function"

**正确的理解**：
- TD3-BC 的整体架构不动
- 只动 critic 里"Q 是什么"——从离散 Bellman 的 Q 换成教授给的连续时间 Q
- 不要扯论文的 Galerkin、$\max_a$、Algorithm 1 那一套

### 弯路 3：往 v1 实现里塞二阶 Σ 项、Hessian 等论文细节

本次 Claude 在讨论 v1 实现细节时，建议加入二阶项 `½ Σ̂ : ∇_s^2 V`、保留
`phys_idx=[8,9,10]` 截断、用 autograd 算 Hessian 等。

**被否定的原因**：用户原话——
> "你目前干的事情让我感觉非常的 confuse，包括你用加的这些二阶项"

**教训**：
- 在用户尚未确认大方向之前，不要往里堆论文里的细节
- 二阶 Σ 项是 PhiBE-Q 论文的内容，**教授说论文是 later**，所以这周不该出现
- 应该回到最朴素的"教授给的 v / Q 公式"（HANDOFF §5.2），按那个公式做泰勒展开就停

---

## 4. 当前会话的最终落点

用户问的最朴素问题：**"换了 value function 之后会发生什么？"**

本次 Claude 给出三层"会发生什么"的回答：

**事情 1（最浅）：超参意义变了。**
- 你不再调 γ=0.99，你调 β 和 Δt
- β = -ln(0.99)/3 ≈ 3.35e-3 /min 是有物理意义的"每分钟价值衰减率"
- 但**单纯换符号、不动公式形式 → 数学上等价于 baseline**（这正是 Approach A v2 的结果）
- 教授应该不会接受这一层

**事情 2（中等）：target 的形式真的变了。**
- 把 critic target 从 `r + γ Q_target(s', π_target(s'))`
  换成教授给的连续时间 Q 的**泰勒展开式**作为主体：
  ```
  target ≈ r·Δt + e^(-β·Δt) · [Q(s,a) + ∇_s Q · μ̂ · Δt + ½ tr(∇_s^2 Q · Σ̂) · Δt]
  ```
- 关键差别：这次不是 Approach C 那样作为辅助正则，**是 target 本身的主体**
- 用 `μ̂ = (s'-s)/Δt`，只在 `phys_idx=[8,9,10]` 上算
- **HANDOFF §6.6 的 4 层失败诊断对这条路仍可能成立**——SNR、Δt=3min、buffer 单一

**事情 3（最深）：critic 学到的"Q 是什么"变了。**
- 训出的 Q 不再依赖特定 (Δt, γ) 离散化，而是底层 SDE 的真实 Q
- 跨设备/跨采样率通用（CGM 数据可能 1/3/5 分钟不一）
- 这是 PhiBE-Q 论文那一套——**教授说是 later**

**本次 Claude 的猜测**：教授要的是 **事情 2**——
- 事情 1 太浅，教授不会让 "give it some thoughts"
- 事情 3 教授自己说 later
- 事情 2 在中间，需要思考 trick 怎么 apply、但不涉及论文 Galerkin/$Q^*$

**但这只是猜测，用户尚未确认。** 这是当前最重要的悬而未决问题。

---

## 5. 当前悬而未决的问题（按优先级）

### 5.1 最优先：教授的指令到底是事情 1/2/3 哪一层？

- 不能靠猜，需要用户直接问教授，或者用户自己根据对教授风格的理解定调
- **建议下次见教授时直接问**：
  "您说的 'use the same trick' 是指：(a) 只改超参意义不改公式 (b) 把 critic target 换成 Q 公式的泰勒展开作为主体 (c) 实现您论文里的 Algorithm 1？我们觉得是 (b)，但想确认。"

### 5.2 假设是事情 2，"the trick 怎么 apply" 这个思考题

教授邮件 "use the same trick from off-policy learning for the Q defined by the Bellman equation in RL, but apply it to this Q function. Give it some thoughts."

**"the trick from off-policy learning" 是什么？**

在 TD3-BC 现有架构里，off-policy 由这些组件支撑：
- replay buffer 存放任意行为策略生成的 `(s, a, s', r)`
- critic target 是 `r + γ Q_target(s', π_target(s'))`——
  这里 $a' = \pi_\text{target}(s')$ 由 target actor 选，不是 behavior 选
- 所以 target 不依赖 behavior policy → off-policy

**"apply it to this Q function" 怎么应用？**

把上面 target 一行的 `γ Q_target(s', π_target(s'))` 部分，替换成
教授连续时间 Q 公式给出的 target：
```
target = r·Δt + e^(-β·Δt) · E[Q(s_{t+1}, a_{t+1}) | s_t, a_t]
```

由于 actor 是确定性的 (TD3)，$a_{t+1} = \pi(s_{t+1})$，所以：
```
E[Q(s_{t+1}, π(s_{t+1})) | s_t, a_t]
  = E[Q(s_{t+1}, π(s_{t+1})) | s_t, a_t]   ← 期望关于 s_{t+1}
```

这个期望可以用三种方式估：
- **方式 A（最朴素）**：直接拿 sample 的 `s'`，估 `Q_target(s', π_target(s'))`。
  这正是 Approach A 已经做的，等价于 baseline，没意思。
- **方式 B（泰勒展开作为 target 主体）**：用泰勒展开式估期望——这就是事情 2 那条路
- **方式 C（不估期望，估 Bellman residual）**：这是 PhiBE-Q 论文 Algorithm 1 的路——
  教授说 later

**所以 "the trick" 落地后的真正问题是**：
**用什么形式估 `E[Q(s_{t+1}, a_{t+1}) | s_t, a_t]`？**

这正是用户出差期间最值得想的事——不需要代码、只需要纸和笔/脑子。

### 5.3 假设是事情 2 通了，HANDOFF §6.6 的 4 层失败诊断对新方案是否仍成立？

- **第 1 层（drift 估计噪声）**：仍可能成立。$\hat\mu=(s'-s)/\Delta t$ 在 Δt=3min 下
  被 σ√Δt 主导。**但新方案是把它放在 target 主体而非辅助正则**——
  影响方式不同，需要分析
- **第 2 层（状态非全物理）**：保留 `phys_idx=[8,9,10]` 截断即可缓解
- **第 3 层（餐食是脉冲非 SDE 高斯白噪声）**：仍成立。这是数据本身的问题
- **第 4 层（buffer 单一）**：仍成立。PID demo 是确定性策略，behavior 多样性差

**重要**：即使事情 2 正确实现，在血糖数据上**可能仍然失败**，原因不是算法
而是数据。**LQR sanity check 必做**——只要 LQR 跑通，证明实现对了；
血糖上失败可以诚实归因到数据问题。

### 5.4 指示 1 的 (r, β) pair 怎么定？

WEEK_KICKOFF.md 列出的待澄清问题至今没问过教授：
1. Δt 是固定 3 min 还是也可调？
2. "运行得更好"指哪个指标？TIR / TBR / Magni risk / 综合？
3. β 的物理上下界是多少？
4. QUICK 还是 FULL 模式？

**这些问题应该和 5.1 一起问教授**——下次见面一并解决。

---

## 6. 出差期间能干 / 不能干的事

### 能干的（无代码环境也行）

1. **想清楚 trick A/B/C 三种估期望的方式哪个对**——
   纸和笔就够，本次 Claude 给的事情 2 那条路最值得推导
2. **把"换 value function 后会发生什么"的事情 2 数学严格写出来**——
   一阶版本（先不要二阶 Σ），写成 1-2 页推导
3. **诚实评估 HANDOFF §6.6 的 4 层诊断对新方案的适用性**——
   纯思辨工作，不需要代码
4. **准备一份 1 页 memo 给教授**，包含：
   - 我们对邮件 trick 的理解（事情 2）
   - 我们想推导出来的 target 公式
   - 我们的疑问（5.1、5.4 列出的）
   - 下次见面要确认的事
5. **读完 HANDOFF.md 全文**（用户上次说"先看一眼就开始干"，
   有些细节没真正吃透）

### 不能干 / 不应该干的

1. **不要写代码**——出差环境不稳定，也没有 GPU/CPU 资源跑实验
2. **不要再讨论 PhiBE-Q 论文 Algorithm 1**——教授说 later
3. **不要把二阶 Σ 项、Hessian、autograd 这些细节往里堆**——
   v1 还没定，提前讨论 v2 只会增加 confusion
4. **不要做新的方向探索**——HANDOFF §12 第 1-3 条不要踩

---

## 7. 仓库当前状态（push 后）

- 远程：https://github.com/hyuan21/glucose_control
- 本地领先 1 commit（WEEK_KICKOFF.md），本次 push 会带上：
  - `SESSION_2026-05-14.md`（这份）
  - 可能还有 `Comparison_CT.ipynb` 的未提交改动（视用户决定）
- 不在 git 里（gitignored）：
  - `Models/`、`Replays/`、`*中期报告*.docx`

---

## 8. 给下一个会话 Claude 的几条嘱咐

1. **首先承认你不是上一个会话的 Claude**——上一个会话犯过几个错（见第 3 节），
   你需要避免重蹈覆辙
2. **不要重复"展开 Δa = J_π Δs"那条路**——已经被教授和用户明确否定
3. **不要假设教授要的是 PhiBE-Q 论文 Algorithm 1**——教授说 later
4. **承认教授指令的模糊性**——三层"会发生什么"教授到底要哪一层不清楚，
   不要替用户拍板
5. **优先帮用户准备给教授的问题清单**（5.1、5.4），而不是急着推数学或写代码
6. **保持中文交流**，参考 HANDOFF.md 第 12 节和 WEEK_KICKOFF 的"用户偏好"
7. **如果用户已经回到办公室、有代码环境，再讨论代码实现**——
   出差期间专注于想清楚 trick

---

**结尾**：用户出差期间，如果在新设备上重新打开 Cowork，请按 §0 的开场白
向新会话的 Claude 介绍状况。新 Claude 读完三份文档（HANDOFF + WEEK_KICKOFF + 本份）
后应该等同于参与了之前所有讨论。

祝出差顺利，路上想清楚 the trick。

— 本次会话 Claude 留，2026-05-14
