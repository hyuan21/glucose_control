# Session 2026-06-11 — End of 5/22-6/11 Push, Ready to Re-Start

> **接续 HANDOFF.md → WEEK_KICKOFF.md → SESSION_2026-05-14.md。**
>
> 这份文档是 5/22–6/11 期间所有工作的总结。
> 用户准备在新的聊天框继续科研，把这份文档完整粘到第一条消息里。
>
> **最后更新**：2026-06-11

---

## 0. 给下一个会话 Claude 的开场白（复制粘贴）

```
你好。我接续了之前几个会话的项目（offline RL 做血糖控制），
完整的项目历史按这个顺序读：

(1) HANDOFF.md           — 项目最早的历史、Approach A v2 baseline 等价、Approach C v1-v3 HJB 失败
(2) WEEK_KICKOFF.md      — 5/14 教授给的两条本周指示
(3) SESSION_2026-05-14.md — 5/14 那次会话推了 Path B 公式但没动代码
(4) SESSION_2026-06-11.md — 这一份，5/22-6/11 实际把 Path B 落地 + 7 个变体实验 + meeting 总结
(5) RESEARCH_POSITION.md  — 我们攻击的是 Zhu 论文 §3.3.2 留下的 DNN-stability open problem
(6) PATH_B_MATH.md        — Path B 的完整数学推导，公式 (1)-(6) 链式
(7) MEETING_SUMMARY_2026-05-25.md — 5/25 给教授的 meeting 提纲

读完后告诉我：
(a) 你打算怎么帮我继续推进
(b) 你建议下一步先做什么
(c) 有什么具体问题需要我先回答
```

---

## 1. 5/22-6/11 期间做了什么 —— 一句话总结

**把 Path B 完整落地了，跑了 7 个变体，确认教授论文 §3.3.2 的 open problem 是真问题，spectral_norm 给出了一个 principled 解决方案但 TIR ceiling 在 ~57%，意外发现 reward normalization 让 baseline 跳到 74.3% 超过 paper adult 70%。**

---

## 2. Path B 是什么

教授连续时间 Q 公式：

$$
Q(s_t, a_t) = r \cdot \Delta t + e^{-\beta \Delta t} \cdot \mathbb{E}[Q(s_{t+1}, a_{t+1}) | s_t, a_t]
$$

对右边期望做泰勒展开（停在 ∇²_s Q，按教授白板上的位置），得到 critic target：

$$
\text{target}_i = r_i + e^{-\beta\Delta t}\Big[Q_\text{target}(s_i, a_i) + \Delta s_i^\top \nabla_s Q + \Delta a_i^\top \nabla_a Q + \tfrac{1}{2}\Delta s_i^\top \nabla_s^2 Q \, \Delta s_i\Big]
$$

TD3-BC 的整体架构（target net、actor、BC 项、buffer）不动，**只换 critic target 一行**。

详见 PATH_B_MATH.md。

---

## 3. 7 个变体的完整结果

| # | 配置 | TIR | 训练稳定性 | 主要诊断 |
|---|---|---|---|---|
| 1 | Path B neural critic 50k | 59.69% | ❌ critic loss 30x 暴涨 | ∇_s Q 无界增长 |
| 2 | Path B neural critic FULL 100k | **5.46%** | ❌ runaway 完成 | actor 学坏 |
| 3 | Path B + clip=10 (raw reward) | 54.19% | ⚠️ 撞 cap | clip 削掉信号 |
| 4 | Path B + clip=10 + normalize | 41.29% | ⚠️ 撞 cap | normalize 让 \|Q\| 小，clip 相对太严 |
| 5 | Path B + clip=0.5 + normalize | 14.04% | ⚠️ 撞 cap | clip 太紧 |
| 6 | **Path B + spec_norm(Lip=1) + normalize** | **56.96%** | ✅ **完全稳定** | Lip=1 让 Q 表达力受限 |
| 7 | Path B + spec_norm(sn_scale=5) + normalize | 36.54% | ⚠️ | 放宽方向错了 |
| 8 | Path B + linear_basis + normalize | 3.71% | ⚠️ actor loss=-2.4 | \|Q\| 失控带歪 actor |
| 9 | Path B + linear_basis + normalize + fixed_lambda | 2.19% | ❌ actor loss=-817 | fixed lambda 也救不回来 |

**对照基线**：
- PID（HANDOFF）：59.50%
- Baseline 50k×1 同 seed：63.65%
- Baseline FULL 100k×3：67.48% ± 3.16%
- Emerson paper adult Table 3a：70.0%
- **Baseline + reward normalize 50k×1：74.29%** ★（超过 paper！）

**Path B 的 TIR ceiling = 56.96%（实验 6）。**

---

## 4. 两个核心发现

### 4.1 教授论文 §3.3.2 的 open problem 被实测确认

教授 PhiBE-Q 论文 §3.3.2 第二条 remark 明确说：

> "As in classical RL, convergence guarantees are generally unavailable under nonlinear function approximation."

§5 conclusion 列为 future work。

**我们实测确认**：neural critic 下 ‖∇_s Q‖ 在训练中无界增长（44→925 in 100k 步），actor 跟着崩。**这正是论文留下的 open problem。**

### 4.2 Spectral Normalization 是 principled 的解决方案

给 critic 每层 Linear 加 spec_norm → Lipschitz=1 自动成立 → 训练完全稳定，‖∇_s Q‖ 全程在 0.88。

理论对应：spec_norm 把 Yuhua Lemma 3.1 在 linear basis 下用的 Lipschitz bound（c₁, c₂, c₃）作为 architecture constraint 强加到 neural network 上。

**代价**：Lip=1 让 Q 表达力受限，TIR 上限被压在 ~57%。

### 4.3 意外大新闻：reward normalization 让 baseline 跳到 74.3%

把 buffer reward 做 z-score 归一化（保留 reward shape，只改尺度）：

- Baseline 50k：63.65% → **74.29%**（+10.6%）
- 超过 Emerson paper adult Table 3a 的 70.0%

**这跟 Path B 没关系**——是个独立工程改造。但它**纠正了 HANDOFF §3 那个 78-83% 的误记**（实际 paper 是 70%）+ **解释了为什么我们 baseline 一直在 67% 区间**：

- TD3-BC 的 `lambda = alpha / |Q|.mean` 平衡 BC vs Q
- 论文 alpha=2.5 在 D4RL setting 下（reward 量级 ~1）合理
- 血糖 Magni reward 量级几十到几百 → |Q| 大 → lambda 小 400 倍 → BC 项主导
- 归一化让 reward std=1 → |Q| 回到 D4RL 量级 → lambda 回到合理值

---

## 5. 5/25 跟 Yuhua 教授 meeting 的总结（推迟过一次）

完整提纲见 `MEETING_SUMMARY_2026-05-25.md`。

**核心问题准备**（教授可能问）：

**Q：你怎么知道 Path B 实现是对的？**
A：`test_path_b_math.py` 用完整 11×11 Hessian 跟 HVP 实现对照、误差 < 1e-5。

**Q：spec_norm 真的对应论文 Lemma 3.1 吗？**
A：Lemma 3.1 需要 c₁, c₂, c₃ 在 linear basis 下有界，spec_norm 把这件事从 architecture 层强加。本质是 Lipschitz 约束。

**Q：为什么 linear_basis critic 反而崩了？**
A：basis 让 ∇_s Q 自动有界（Lipschitz 性质回来了，二阶项第一次非零），但 |Q| = θᵀΦ 随 |θ| 增长，TD3-BC 的 `lambda = alpha/|Q|` 失稳，actor 抛弃 BC 完全追 Q → 学坏。我们试了 fixed_lambda（lambda = alpha/reward_std）也没救回来。

**Q：reward normalization 是不是改了 reward function？**
A：**不是**——shape 完全保留（低血糖 7 倍惩罚、terminal -1e5 都在）。只是 z-score 缩放。

**Q：下一步打算怎么办？**
A：等教授指示。三个候选方向：
- 继续在 Path B 上找别的稳定化（gradient penalty、L2 reg、orthogonal init...）
- 把连续时间公式用在别的位置（不是 critic target 主体，而是 actor 改进的 ∇_a Q 信号、或 OOD 诊断、或跨 patient 迁移）
- 把 reward normalization 作为独立研究方向

---

## 6. 当前代码状态

GitHub: https://github.com/hyuan21/glucose_control
最新本地 commit：3da3300（5/14 push 过）
**5/22-6/11 这一大批改动都还在 working tree 没 commit**——需要在新会话第一件事 commit + push。

### 6.1 主要代码改动

**`TD3_BC_ct.py`**（核心 critic 训练，~865 行）：
- 加了 `LinearBasisCritic` 类（Q = θᵀΦ，39 个 features）
- `Critic` 类支持 `use_spectral_norm` + `sn_scale`（per-layer Lipschitz lift）
- `__init__` 加了几个 flag：`use_path_b`、`normalize_reward`、`use_spectral_norm`、`sn_scale`、`critic_type`、`lambda_mode`、`path_b_grad_clip`
- train loop 里实现 Path B target（含 HVP 计算二阶项）
- actor update 支持 `lambda_mode='fixed_by_reward_std'`

**`TD3_BC.py`**（论文 baseline）：
- 加 `normalize_reward` 开关（默认 False，保持论文行为）

**`utils/data_processing.py`**：
- 修了 `if reward_mean:` 这个 truthy bug → `is not None`

### 6.2 所有 flag 默认值 = 论文行为

| flag | 默认 | 论文行为？ |
|---|---|---|
| use_path_b | False | ✅ |
| use_hjb | False | ✅ |
| normalize_reward | False | ✅ |
| use_spectral_norm | False | ✅ |
| sn_scale | 1.0 | ✅ |
| critic_type | "neural" | ✅ |
| lambda_mode | "adaptive" | ✅ |
| path_b_grad_clip | 10.0 | ⚠️ 只在 use_path_b=True 时生效 |

**baseline / A v2 / C v1-v3 历史实验路径完全保留可复现**。

### 6.3 实验脚本

- `run_baseline_quick.py` — 论文 baseline 50k×1
- `run_baseline_norm.py` — baseline + normalize 50k×1（出 74.3% 那次）
- `run_path_b_quick.py` — Path B neural 50k×1
- `run_path_b_full.py` — Path B neural FULL 100k×1
- `run_path_b_norm.py` — Path B + normalize（clip=10）
- `run_path_b_norm_clip05.py` — Path B + normalize + clip=0.5
- `run_path_b_specnorm.py` — Path B + normalize + spec_norm(Lip=1)
- `run_path_b_specnorm_scale5.py` — sn_scale=5
- `run_path_b_linbasis.py` — linear_basis critic + fixed_lambda
- `compare_quick.py` — 加载 baseline + Path B 权重并排评估
- `test_path_b_math.py` — Path B 数学层单元测试
- `test_linear_basis.py` — linear_basis critic 单元测试

### 6.4 文档

- `HANDOFF.md` — 项目最早的交接（A、C 失败历史）
- `WEEK_KICKOFF.md` — 5/14 教授两条指示
- `SESSION_2026-05-14.md` — 5/14 那次会话的方向调整
- `SESSION_2026-06-11.md` — **本文档**
- `PATH_B_MATH.md` — Path B 数学推导
- `RESEARCH_POSITION.md` — 我们攻击的 open problem 定位
- `MEETING_SUMMARY_2026-05-25.md` — 教授 meeting 提纲
- `group_meeting_v4.pptx` — group meeting 用的 1 页 PPT
- `build_group_meeting_slide_v4.py` — PPT 生成脚本（出差可改）

---

## 7. 下一步候选方向（按 6/11 之后）

教授 meeting 已经推迟一次。下一步取决于教授指方向，但**我们手里有几个备选**：

**方向 A — 继续 Path B 找别的稳定化**：
- Gradient penalty（WGAN-GP 风格的 `(‖∇_s Q‖-1)²` 软约束）
- L2 regularization on critic weights
- Smoother activation（GELU、Tanh）让 ∇²_s Q 非零
- Orthogonal initialization

**方向 B — 把连续时间公式换个位置用**：
- 不作为 critic target 主体，而是作为 OOD 诊断（训完后测 critic 满不满足教授公式）
- 用 ∇_a Q 信号改进 actor loss（actor 看到 Q 在动作方向的局部梯度）
- 跨 patient 迁移测试（连续时间结构理论上跨 patient 通用）

**方向 C — 把 reward normalization 做成独立研究**：
- 在更多 patient（child、adolescent）上验证 normalize 是否一致提升
- 对比 paper 报告的 child（60.1%）/ adolescent（62.0%）
- 写成 short paper / workshop

**方向 D — 探索教授指示 1**（找 (r, β) pair 的 selection rule）：
- 物理时间尺度匹配：β = 1/τ_system
- 5/14 那次推过几个 framework（PATH_B_MATH.md 第 8.3 节）

---

## 8. 给下一个会话 Claude 的嘱咐

1. **优先级**：在用户给你新指示之前，先把所有文档读完。不要自作主张选方向。
2. **不要重蹈这次的弯路**：
   - 不要假设 Path B 在 neural 下能轻易稳定——已经验证需要 architecture constraint
   - 不要再试 clip on input gradient——撞 cap 这件事我们已经测过 3 种 clip 值
   - 不要假设 linear basis 一定行——|Q| 失控的问题已经实测过
   - 不要把 reward normalization 跟 Path B 混着分析——它们是两件独立的事
3. **承认天花板**：Path B 在我们这个 setting（adult#1 + PID buffer + Magni reward）下的实测 TIR 上限是 57%。这不是绝对 lower bound，但是个稳定的 empirical observation。
4. **保持中文交流**——用户偏好中文讨论数学和工程问题；代码注释 + 论文风格写作可以英文。
5. **GitHub 状态**：本地有大量未 commit 的改动。新会话第一件事建议是 commit + push 把所有 5/22-6/11 工作保留下来。
6. **教授 meeting 还没开**：MEETING_SUMMARY_2026-05-25.md 是当时的准备，但 meeting 被推迟到了之后。如果 6 月有 meeting，用 MEETING_SUMMARY_*.md 准备 + 本文档第 5 节的问答 Q&A。

---

## 9. 用户偏好（这两周观察新增）

- **不要在没确认方向时往里加细节**（二阶 Σ、Hessian、复杂理论——不利于沟通，应等用户拍板）
- **诚实归因负向结果** > 修饰失败为成功
- **每个实验之前先告诉用户预期效果 + 失败时的诊断假设**——这样用户知道我们在测什么
- **复盘对话时承认错误**（这次会话里我多次说"这个方向应该 work"结果不 work，用户提醒我"我们应该有 selection rule 而不是猜"）
- **不要每个问题都列 3-5 个候选答案让用户选**——用户希望 Claude 给一个推荐 + 理由，他来决定接受/否定

---

**结尾**：5/22-6/11 这一段 Path B 路线已经做透。研究价值最大的发现可能是 reward normalization 这条独立线索。下次教授见面或下个月的 push 应该围绕"教授指方向 + reward normalization 这条线索"展开。

祝下一段顺利。— 本次会话 Claude 留，2026-06-11
